<resources>

    <style name="None">
    </style>

    <!-- Base application theme. -->
    <style name="GrommetTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>

        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="colorSecondary">@color/colorAccent</item>
        <item name="colorOnSecondary">@color/white</item>

        <!-- Material Styles-->
        <item name="textInputStyle">@style/Widget.App.TextInputLayout</item>
        <item name="materialButtonStyle">@style/Widget.App.Button.MaterialButton</item>
    </style>

    <style name="Widget.App.Button.MaterialButton" parent="Widget.MaterialComponents.Button.TextButton">
        <item name="android:textColor">@color/text_button_color_selector</item>
    </style>

    <style name="Widget.App.TextInputLayout" parent="Widget.MaterialComponents.TextInputLayout.OutlinedBox">
        <item name="materialThemeOverlay">@style/ThemeOverlay.App.TextInputLayout</item>
        <item name="hintTextColor">@color/colorAccent</item>
    </style>

    <style name="ThemeOverlay.App.TextInputLayout" parent=""/>

    <style name="RegistrationTheme" parent="GrommetTheme">
        <item name="android:statusBarColor">@color/colorPrimaryDark</item>
    </style>

    <style name="GrommetDivider" parent="None">
        <item name="android:layout_height">1dp</item>
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_marginTop">@dimen/content_area_padding</item>
        <item name="android:layout_marginBottom">@dimen/content_area_padding</item>
        <item name="android:background">@color/divider</item>
        <item name="android:layout_gravity">fill_horizontal</item>
        <item name="android:layout_marginStart">@dimen/content_margin</item>
    </style>

    <style name="GrommetContentArea" parent="None">
        <item name="android:layout_gravity">start|fill_horizontal</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:layout_marginStart">@dimen/content_margin</item>
        <item name="android:layout_marginEnd">@dimen/content_margin</item>
    </style>

</resources>
