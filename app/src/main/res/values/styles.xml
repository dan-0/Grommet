<resources>

    <style name="None">
    </style>

    <!-- Base application theme. -->
    <style name="Theme.Grommet" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowActionBarOverlay">true</item>

        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="android:spinnerStyle">@android:style/Widget.Material.Spinner.Underlined</item>
    </style>

    <style name="Grommet.Divider" parent="None">
        <item name="android:layout_height">1dp</item>
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_marginTop">@dimen/content_area_padding</item>
        <item name="android:layout_marginBottom">@dimen/content_area_padding</item>
        <item name="android:background">@color/divider</item>
        <item name="android:layout_gravity">fill_horizontal</item>
        <item name="android:layout_marginStart">@dimen/content_margin</item>
    </style>

    <style name="Grommet.Content_Area" parent="None">
        <item name="android:layout_gravity">start|fill_horizontal</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:layout_marginStart">@dimen/content_margin</item>
        <item name="android:layout_marginEnd">@dimen/content_margin</item>
    </style>

    <style name="Grommet.Content_Area_Left" parent="None">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_gravity">start|fill_horizontal</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:layout_marginStart">@dimen/content_margin</item>
    </style>

    <style name="Grommet.Content_Area_Right" parent="None">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_gravity">start|fill_horizontal</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:layout_marginEnd">@dimen/content_margin</item>
    </style>

</resources>
